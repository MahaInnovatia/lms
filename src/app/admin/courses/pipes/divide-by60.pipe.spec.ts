import { DivideBy60Pipe } from './divide-by60.pipe';

describe('DivideBy60Pipe', () => {
  it('create an instance', () => {
    const pipe = new DivideBy60Pipe();
    expect(pipe).toBeTruthy();
  });
});
